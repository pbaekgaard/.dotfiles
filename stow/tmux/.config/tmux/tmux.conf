set -g mouse on
set -gq allow-passthrough on
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on
set -gw window-status-current-style bold
# Set prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix
unbind h
unbind l
bind-key h previous-window
bind-key l next-window
bind-key q kill-session
bind-key r command-prompt -I "#W" "rename-window '%%'"
bind-key -r C-f run-shell "tmux neww ~/.local/bin/tmux-sessionizer"
bind -n C-M-h select-pane -L
bind -n C-M-j select-pane -D
bind -n C-M-k select-pane -U
bind -n C-M-l select-pane -R
# Plugin list
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'o0th/tmux-nova'
# set -g status-left-length 100
# set -g status-right-length 100
# set -g @nova-nerdfonts true
# set -g @nova-nerdfonts-left 
# set -g @nova-nerdfonts-right 
# set -g @nova-segment-mode "#{?client_prefix,Ω,ω}"
# set -g @nova-segment-session "#{session_name}"
# set -g @nova-segment-session-colors "#689d6a #181c2a"
# set -g @nova-segment-date "#(date +%H:%M)"
# set -g @nova-segment-date-colors "#ebdbb2 #181c2a"
# set -g @nova-segment-mode-colors "#689d6a #181c2a"
# set -g @nova-status-style-double-bg "#2d3540"
# set -g @nova-status-style-active-bg "#2d3540"
# set -g @nova-status-style-active-fg "#fabd2f"
# set -g @nova-status-style-bg "#181c2a"
# set -g @nova-status-style-fg "#ac7f16"
# set -g @nova-segment-whoami "#(whoami)@#h"
# set -g @nova-segment-whoami-colors "#c9bb99 #181c2a"
#
# set -g @nova-pane "#I#{?pane_in_mode,  #{pane_mode},}  #W"
# # set -g @nova-pane-active-border-style "#44475a"
# # set -g @nova-pane-border-style "#181c2a"
# set -g @nova-segments-0-left "session"
# set -g @nova-segments-0-right "whoami date"
set -g @plugin 'TechnicalDC/tmux-ayu-theme'
set -g @plugin 'jimeh/tmuxifier'
run '~/.config/tmux/plugins/tpm/tpm'
